import { Profile } from './_components/profile';

export const metadata = {
  description: 'Profile',
  title: 'Profile',
};

const ProfilePage = () => {
  return <Profile />;
};

export default ProfilePage;
