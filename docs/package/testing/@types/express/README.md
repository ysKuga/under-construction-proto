# @types/express

<https://www.npmjs.com/package/@types/express>

## 概要

`express` の型定義パッケージ。`mock-server.ts`(MSW のモックサーバー実装、`test-e2e` 用)で使用する `express` に型を付与する。

## バージョン注意

`express` は 4 系を使用しているため、`@types/express` も 4 系(`^4.17.25`)を選定した。`@types/express` 5 系は `express` 5 系向け。
