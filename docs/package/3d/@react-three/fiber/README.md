# @react-three/fiber

<https://r3f.docs.pmnd.rs/>

## 概要

[three](../../three/README.md) を React のレンダラーとして扱うためのライブラリ。`<Canvas />` 配下に `<mesh />` などを JSX で記述する。

## バージョン注意

v9 系は React 19 が peer dependency。本プロジェクトは React 18 のため v8 系を使用する。
