# JSDoc

変数、基本的に JSDoc で簡潔な説明付与する。

## 例外

戻り値の型が export された型で、かつ型定義側(プロパティ単位含む)に JSDoc 付与済みの場合、ホバー時にその説明が反映されるため、変数宣言側の JSDoc は省略可。

- 例: [プロパティ JSDoc](../../CLAUDE.md) ルールに従い各プロパティへ JSDoc 付与済みの型を返す hook の戻り値
- 上記に該当しない場合(プリミティブ型 `string`/`number`/`boolean` 等、または JSDoc 未付与の型)は変数宣言に JSDoc 必須
