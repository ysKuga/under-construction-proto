# 決定事項（エネルギー）

- 2026-09-16: 「歩数制限」から「エネルギー」へ改称。ローグライク的な燃料ゲージを想起しやすい命名（コード上の変数名も energy 系に合わせる）
- 2026-09-16: 消費・回復方式は「実行中消費型」。予定経路は自由に組め、「実行」tick 進行ごとに1消費、道中の回復要素で回復する。「事前判定型」（予定経路作成時点で上限超過を選択拒否）は、まだ実行していない時点で回復アイテムを「拾った」前提の計算が必要になり不自然なため不採用
- 2026-09-16: 回復手段は2種。道中に落ちている回復アイテム（踏むと回復）／固定セルの回復スポット（ガソリンスタンド的、到達で回復）
- 2026-09-16: 略称は「EN」。UI 表示等で使う。今後 PJ 全体として、概念には簡潔な略称を用意していく方針
- 2026-09-16: EN はステージでなく actor（box-bot）に属する値（ローグライク的にキャラクター側の資源とする）。注入は Context 経由とし、props 追加による変更波及を避ける。汎用的な注入手段の整備自体は本 issue のスコープ外、別途プロジェクト全体の課題として検討
- 2026-09-16: 回復スポットは固定量回復を複数回（在庫を持ち枯渇しうる、ガソリンスタンドの在庫的な挙動）
- 2026-09-16: 回復アイテムは固定量だが1個で複数手分回復する量（現実の「1食で数時間活動できる」相当の量感を想定）
- 2026-09-16: EN 残量の UI 表示は数値表示（`EN: 5/10`）
- 2026-09-16: 実装対象は proto-01・proto-03 両方。proto-01 は「実行」tick 駆動（`useFindPathTick`）が既存のためそのまま統合、proto-03 は tick 駆動自体が未実装（隣接クリック逐次移動のみ）のため新設が必要。両者を分離し、proto-03 側は別 PR とする
- 2026-09-16: EN store は find-path 固有の zustand store として新設（`FindPathStoresProvider` と同じ `createStoreContext` パターン）。time-control-03（汎用時間管理ロジック）へは持ち込まない、EN はゲームデザイン上の資源管理概念のため
- 2026-09-16: PR #183 で proto-01 の EN 保持・Context 注入・消費ロジック（`useFindPathTick` への統合、0 で打ち切り）を実装。回復ロジックは別 PR
- 2026-09-16: 略称「EN」は画面表示専用（3行目の方針を再徹底）。型・変数・store・関数等の実装識別子は正式名称 `energy` を使う（`EnStore` 等の略称実装を `EnergyStore` 等へ修正）。JSDoc コメントも「エネルギー」表記に統一する
- 2026-09-17: EN 残量 UI（`EN: x/y`）を proto-01 の `ActionBar` へ実装。専用コンポーネント新設でなく既存 `ActionBar` へ `useEnergyStore` selector で表示（`usePlannedPathStore`/`useGameClockStore` と同じ配置パターン）。Storybook + Playwright で「実行」による消費 → 表示反映（10/10 → 9/10）を確認
- 2026-09-17: proto-03（hex）への tick 駆動「実行」導入は規模が大きいため PR-A（stage-07 基盤: `interactive` prop + hex 版 ActorNodeRegistry）→ PR-B（EnergyStore を proto-01 から find-path 共有層へ移設）→ PR-C（hex tick駆動+EN配線+UI）の3段階に分割する
- 2026-09-17: PR-A 実装。stage-06 の `ActorNodeRegistryProvider`（DOM 直書き）とは異なり、stage-07 版は `useState` ベースの Context に留める（stage-07/proto-02/03 は元々 props 経由で再レンダリングする設計のため、DOM 直書きへの書き換えは不要な複雑化と判断）。`Stage07Props.initialCell` は廃止し `ActorNodeRegistryProvider` 側の prop へ移設。`Stage07Props.interactive`（既定 `true`）を新設し、hex 版 `GeoLayer` を stage-06 と同じ button/div 分岐にした
- 2026-09-17: PR-B 実装。EnergyStore（constants/context/index/store/types）を `find-path/_prototypes/proto-01/_stores/energy` から `find-path/_prototypes/_stores/energy`（proto-01/02/03 共有、`_prototypes` 配下に留める）へ移設。find-path ページ自体（route/page）は未実装のため `_prototypes` の外（`find-path/_stores`）へは出さない。proto-01 側の import は絶対パス（`@/components/pages/find-path/_prototypes/_stores/energy`）へ変更。PR-C で proto-03 が同 store を参照できるようにするための下準備、挙動変更なし
- 2026-09-17: PR-C（proto-03 tick駆動+EN配線）は複数マス選択（予定経路）機能を含めて実装したが、選択済みセルのフェードアウト表示が tick 消化に追従せず revert（PR #188、docs/concept/implementation/multi-cell-selection/README.md）。再実装時は複数マス選択をスコープ外とし、既存の隣接クリック逐次移動方式のまま tick 駆動「実行」+ EN 消費配線のみ対応する。フェードアウト表示機構（DOM 直書き移植 or store 設計作り直し）の検討は複数マス選択を再着手する際まで持ち越し
- 2026-09-17: proto-03 の EN 配線を実装。tick 駆動「実行」自体は導入せず、既存の隣接クリック逐次移動（`useHexMove`）へ直接組み込んだ。`EnergyStoreContext.Provider` を `ActorNodeRegistryProvider` の外側に配置し、`canEnterCell` へ EN 残量 >0 判定を追加、移動成立時（`onCellChange`）に EN 1 消費する。Storybook + Playwright で10手移動後の EN 切れ・進入不可を確認
- 2026-09-17: EN 切れ時の演出は専用新規 action（box-bot `_actions/` 配下、仮称 `energy-out`）として実装する方針。既存 `fall` action（前傾・腕引き寄せ・影持ち上げ・`postureRef` 姿勢フェーズ管理）とは役割を分ける。理由: 「1手戻す」等の別文脈でも転倒姿勢を使う可能性があり、fall 流用だと意味(なぜ倒れたか)が混在する。EN 切れ専用の見た目・トリガー条件を独立させておく方が今後の調整が楽
- 2026-09-17: `energyOutAction` を実装（`box-bot-01/_actions/energy-out/`）。`useFindPathTick` の `applyNextStep` から `outOfEnergy` 成立時に発火、proto-01 の `Stage06 actorActions` / `useBoxBotActionDispatcher` へ配線済み。`postureRef` は共有しない（EN 切れは tick 停止と同時に起きるため walking/marching 側の抑止は tick 停止で足りる）
  - 初版は「体を縮める(`applySquash`)のみ」で実装したが、EN 切れは経路実行中に検知できる(不意の事故でなく想定内の停止)という前提に合わないため撤回。「転倒を防ぐための予防姿勢・動力を使わない安定した姿勢」というコンセプトへ変更、胴体を少し前傾 + 腕を垂らすの組合せへ作り直した
  - 2 版目(`applyTiltAngle`(fallPivotRef、シルエット中心軸)+ `applyArmAngle`(x 軸、肩を支点に前へ振る))はレビューで却下。「腕を垂らしていない(前に出ているだけ)」「脚も一緒に傾いてしまう」の指摘。原因: `applyTiltAngle` は fall と共有のシルエット中心軸で脚を含む全身が回転する。`applyArmAngle` は x 軸(前後振り)で、肩を支点に回すと前方水平に持ち上がって見え「垂らす」にならない
  - 3 版目で box-bot-model 共通構造を拡張: 腰(脚の付け根)を支点に上半身(胴体・頭・腕・目口)だけ前傾させる `torsoRef`(新規、`walkingBobRef` の内側・脚とは兄弟)/ `applyTorsoTiltAngle` を追加(`BoxBotRefs`/`BoxBotActionHost`/`box-bot-model/index.tsx` の JSX を変更)。腕は `applyArmLift`(z 軸、arm-toggle と共有の操作面)で静的な肩の開き(`DEFAULTS.arm.leftAngle` -0.5 / `rightAngle` 0.5)を打ち消す方向へ左右対称に加えたが、これは「体(torsoRef)に対して垂直」止まりで、腕は torsoRef の内側にネストされているため胴体の前傾ぶん地面に対しては傾いたまま
  - 4 版目で修正: 「地面に対し垂直(転倒しかけたときすぐ支えに使える準備姿勢)」という要求に合わせ、`applyArmAngle`(x 軸)へ胴体前傾と逆符号の角度を追加し、胴体の前傾を打ち消して腕が地面に対し鉛直を保つようにした。Playwright で `legLeftRef`/`legRightRef` の `rotation.x` が 0(脚は傾かない)、腕(肩の開きを持つ内側 group)のローカル `-y` 方向をワールド変換した結果が `(0, -1, 0)`(完全な鉛直)であることを数値で確認済み
  - 5 版目(現行)で `ENERGY_OUT_ARM_LIFT` を 0.5(静的な肩の開きを正味 0 まで打ち消し、完全に体側へ寄せる)→ 0(静的な開きをそのまま残す)へ変更。「手が埋もれてしまっている」の指摘。0.5(体側にぴったり沿う)はもちろん、0.2 程度に弱めても体の陰にほぼ隠れて見えにくかった。`applyArmLift`(z 軸、開閉)と `applyArmAngle`(x 軸、鉛直補正)は独立した軸のため、z 軸を 0(操作しない)のままにしても x 軸側の鉛直補正には影響しない
- 2026-09-17: EN 回復ロジック(回復アイテム/回復スポット)を実装。回復要素の状態(未回収フラグ・スポット残量)は energy store へ混在させず、proto-01 固有の汎用 `ItemStore`(`_stores/items`)として新設した。将来アイテム種類が増える想定のため、energy store 専用ロジックへ埋め込まない方針（ユーザー指示）。`ItemInstance` の `cell` は `ActorNodeRegistry` の `GridPosition` 型をそのまま使い、位置の型・判定パターンを共有する
- 2026-09-17: `ItemInstance.stock` の有無で「回復アイテム」（未指定、1個ずつ使い切り）と「回復スポット」（指定、指定回数で枯渇しうる）を区別する。`kind` は種類(`'energy-recovery'`)の判別用に別途持つ（将来の種類拡張用、今回は1種類のみ）
- 2026-09-17: `ItemStore` は proto-01 固有に配置（`proto-01/_stores/items`）。EnergyStore も PR #183 時点では proto-01 固有で作られ、proto-03 対応（PR-B）で共有層へ移設された経緯を踏襲。今回は proto-01 のみ対象のため同じ順序とし、共有が必要になった時点で移設する
- 2026-09-17: 回復判定は `use-find-path-tick` の `applyNextStep` 内、`moveActor` 実行後・`energy.consume` 実行前に挿入（実際にそのセルへ進入できた場合のみ回収、障害物で進めない場合は回収しない）
- 2026-09-18: 回復アイテムの携行可能化（design.md 懸念・リスク）を実装。回復アイテム（`stock` 未指定）は踏んでも即時回復せず、専用 `CarriedItemStore`（proto-01 固有、上限3個・仮値）へ pickup する。回復スポット（`stock` 指定）は据置型のため対象外、従来通り即時回復のまま。使用タイミングはプレイヤーが選ぶ手動方式を採用（自動使用は「いつ使うか」の選択肢がなくなるため不採用）。携行は最古優先（FIFO）で `useCarriedItem`（`ActionBar` の「使用」ボタン）から1個ずつ消費する。上限に達している場合はアイテムをその場に残す（`ItemStore` から消費しない）。UI は `EN: x/y` の横に携行数（`携行: n/上限`）・回復スポット残り在庫（`スタンド: n/初期在庫`、現状スポットは1箇所のみのため単一表示）を追加
- 2026-09-18: box-bot 側 EN 回復ロジック（見た目演出）を実装。既存 `energyOutAction`（EN 切れ→予防姿勢、issue #181 2026-09-17）はトグル式で「予防姿勢で静止中に再発火すると復帰」まで実装済みだったが、`use-find-path-tick` 側は EN 切れ時の発火のみ配線されており、回復時の復帰発火が欠けていた。`outOfEnergyRef`（発火中フラグ）を追加し、EN 切れで true・以後の回復発生時（tick 停止後の再「実行」で回復アイテムのマスへ到達した場合も含む）に再度 `energyOut()` を dispatch して false に戻す方式で配線。ユニットテスト3件追加、Storybook + Playwright で EN 切れ時の前傾姿勢・回復後の直立復帰を実機確認
- 2026-09-19: 回復アイテム/回復スポットを proto-03（hex）へ移植（issue #137 の「開発優先は proto-03」方針に対し、`_stores/items` を proto-01 でしか実装しておらず新規機能を proto-01 側で継続してしまった反省を受けて着手）。`ItemStore`/`_lib/get-cell-contents.ts`/`_lib/item-presentation.ts`/`_components/item-layer/`（`ObstacleLayer` 等と同型）を axial 座標へ移植した proto-03 固有実装として複製。EnergyStore（PR-B）と異なり、proto-01 側は携行可能化（issue #137 PR #196 派生）が別途進行中で共有層への統合は時期尚早と判断、当面は複製のまま。proto-03 は予定経路・tick 駆動を持たないため即時使用のまま（携行可能化は対象外、`index.tsx` の `handleCellChange` で即時回復）。title/className 表示（issue #137、`CellTitleProvider`/`getCellContents`/`describeCellContent`）も同時対応
- 2026-09-20: EN 切れ時の姿勢確認（PR #197）中、EN 切れ演出開始時に脚が一瞬傾いて見える不具合を発見。原因は `use-find-path-tick` の `applyNextStep` が EN 消費（energy store 直接操作）・EN 切れ判定・`energyOut()`（box-bot action 直接 dispatch）を一括で担っており、歩行トグル OFF（`walking()`）の脚減衰アニメーションと `energyOut` の上半身前傾アニメーションが同時進行するため。この密結合自体を解消する方針へ転換（今後 EN 以外の状態変化・アクションが増えても tick 処理側にロジックが積み上がらないようにするため）: `use-find-path-tick` は `CONSUME_ENERGY` イベントを dispatch するだけにし、energy store 側の新設 `_event-hooks/use-consume-energy-event-listener`（`_stores/energy/_event-hooks/`、time-control-03 の `_events/_event-listeners/use-xxx-event-listener/` と同型）が購読して `store.consume` を実行、`current<=0` なら `ENERGY_DEPLETED` を発行する。`ENERGY_DEPLETED` は `FindPathContent` 配下の新設 bridge hook が購読して `energyOut()` を dispatch する（tick 処理は box-bot action を直接知らなくなる）。EventTarget は box-bot 側の `actorEventTarget` と分離し、find-path 専用に新規生成する。tick 継続可否の `outOfEnergy` 判定（`rest.length===0 || outOfEnergy`）は「読むだけ」のため対象外、引き続き `energy.getState().getEnergyInfo(...)` を直接読む。回復側（`items` 消費 → `energy.recover` → `energyOut` 復帰トグル）は今回スコープ外、直接呼出しのまま据え置く（携行アイテム化・pickup/use 分離の検討時に合わせて再設計、design.md 懸念・リスク）
- 2026-09-20: 上記不具合の実際の原因は event 疎結合化の対象範囲外だった。`walking`（トグル OFF）の脚・腕は `settleRate`/`speedApproachRate` による自然減衰で戻るが、角速度の減衰（`speedApproachRate=3`）が律速し実測で 1 秒以上歩行の余韻（sin 波の位相進行）が残る。この間に `energyOut` の前傾が始まるため「全体が傾斜 → 足が垂直に戻る」ように見えていた（`setTimeout` で `energyOut` 発火を待たせる案も試したが、300ms 程度では自然減衰が全く収まらず不十分と実測で確認）。修正: `walking` OFF と同時に `walkingReset`（引数省略で脚・腕を即時スナップ）を発火し、脚が振れたまま前傾が始まる経路自体を塞いだ。その後 `ENERGY_OUT_DELAY_MS`（300ms、演出上のタメ）を挟んで `energyOut` を発火する構成は残す（`walkingReset` で脚は既にスナップ済みのため、遅延中に振れたまま残る心配がない）。`use-find-path-tick` に `walkingReset` dispatcher を追加、`proto-01/index.tsx` の `actorActions`/`useBoxBotActionDispatcher` へ `walkingResetAction` を配線した
- 2026-09-20: 上記デバッグ中、EN 上限（`DEFAULT_ENERGY_INFO`）を一時的に 10→3 へ書き換えて EN 切れまでの手数を減らし検証した（確認後 10 へ戻し済み）。境界値（EN 切れ等）の確認を都度ソース書き換えで行うのは非効率なため、今後は EN 残量を調整できる UI・リセットボタンを prototype に用意する方針とした（backlog.md 追加）。find-path の変更主対象は proto-03（`_prototypes/CLAUDE.md`）のため、これらの UI も proto-03 側に用意する想定。issue-181-en の当 PR（#197）では対応不要
- 2026-09-21: EN 残量調整デバッグパネルを実装。proto-01・proto-03 両方で使うため `EnergyDebugPanel`（`_prototypes/_components/energy-debug-panel/`、energy store と同じ共有配置）として新設し、両 proto の `index.tsx` へ配線した。-1/+1（`consume`/`recover` を 1 ずつ）で任意の残量を作れるようにし、専用の「set」store メソッドは追加せず既存 `consume`/`recover`のみで足りる構成にした。単独マウント不可（`EnergyStoreProvider` 配下必須）のため story は追加せず。Storybook + Playwright で両 proto の表示・-1連打による0クランプ・+1の反映を確認
- 2026-09-21: 上記の「リセットボタン」は EN store 単体でなく、ゲーム全体（position/path/items 等の全 store）のリセットを指すユーザー指摘を受けスコープ訂正。`EnergyDebugPanel` からは reset ボタンを削除（-1/+1 のみに縮小）、ゲーム全体のリセットボタンは別途 backlog へ再掲し未実装のまま持ち越す
- 2026-09-21: backlog.md の実装計画順序を並べ替え。proto-03 優先方針（2026-09-19）に対し、回復アイテム配置検討・actor 回復ロジック実装（いずれも proto-01 系）が proto-03 演出実装より先に並んでいたため、event 疎結合リファクタ・境界値テストボタン（演出実装の前提・検証基盤）の直後へ proto-03 演出実装を繰り上げ、proto-01 系の2項目は後段へ
- 2026-09-21: 境界値テスト用リセットボタンを proto-01・proto-03 両方へ実装。`resetAll`（time-control-03）のような store 個別 `reset()` 呼出し積み上げ方式ではなく、Provider 群最外殻（`FindPathStoresProvider`/`EnergyStoreProvider`）へ `resetKey` state を `key` として渡し、更新のたび配下（store・`ActorNodeRegistryProvider` の position ref・`FindPathContent` 内 state 含む）を丸ごと再マウントする方式を採用。proto-01 は関連 store が7つあり個別 reset 呼出しだと store 追加時の呼び忘れリスクがあるため。ボタンは proto-01 `ActionBar`（走行中は disabled）・proto-03 Content 直下（`EnergyDebugPanel` 横）にそれぞれ配置。Storybook + Playwright で両 proto の EN デバッグパネル `-1` 押下 → リセットボタンで `EN: 10/10` へ復帰することを確認
- 2026-09-21: EN 消費 → EN 切れ検知の event 疎結合リファクタを実装（PR #197 由来、2026-09-20 設計）。energy store 配下に `_events/`（`EnergyEventProvider`・専用 `EventTarget`。box-bot 側 `actorEventTarget`・proto-01 `FindPathEventProvider` とも分離）を新設し、`_event-listeners/use-consume-energy-event-listener` が `Energy-consume` を購読して `store.consume` 実行 → 残量 0 以下なら `Energy-depleted` を発行する。`use-find-path-tick` の `applyNextStep` は `Energy-consume` を dispatch するだけになった（tick 継続可否の判定自体は「読むだけ」のため従来通り `energy.getState().getEnergyInfo(...)` を直接読む）。`EnergyStoreContext`/`useEnergyStoreApi`/`useEnergyStore` は `context.tsx` から `_contexts/store-context.ts` へ分離（`context.tsx` が `_events`（consume-listener）を組み込むため、consume-listener 側が `context.tsx` を参照すると循環 import になることを避けるため）
  - 設計変更点: 当初案（2026-09-20）は「`Energy-depleted` は `FindPathContent` 配下の新設 bridge hook が購読して `energyOut()` を dispatch する」だったが、EN 切れ演出の発火中フラグ（`outOfEnergyRef`）は回復トグル（`items` 消費・`useCarriedItem`、今回のリファクタ対象外）と共有する必要があり、これを tick の外（`FindPathContent`）へ分離すると状態共有が破綻する。回復側の実装を変えない制約の中で最小の変更にするため、`Energy-depleted` の購読自体は `useFindPathTick` 内に残し（`outOfEnergyRef`/`energyOut` 呼出しは従来のまま）、tick が担っていた「消費の実処理・閾値判定」だけを energy store 側（consume-listener）へ切り出す形に変更した
  - Storybook（proto-01 Default）+ Playwright で、EN デバッグパネルで残量 1 まで減らした後「実行」→ EN: 0/10・EN 切れ演出（上半身前傾・腕を垂らす姿勢）の発火をスクリーンショットで確認
