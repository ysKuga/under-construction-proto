# 実装計画（中継点経由の経路統合と自動移動）

- [x] 中継点の経由順を最近傍法で決定する（lib 化 + テスト）
  - クリック順でなく、設置箇所が近い同士を接続する
  - 厳密な最適解（TSP）までは求めない
- [ ] 中継点を経由した経路を `PathPreviewLayer` へ統合する
  - 区間ごとに `findHexPath` を呼んで経路を連結する
- [ ] 経路に沿った自動移動を実装する
  - 入力は経路配列にする
  - 開始トリガー（実行ボタン / 確定操作）を決める
  - EN 不足時の扱いを決める
