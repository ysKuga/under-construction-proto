# 決定事項（issue #137）

backlog.md から参照される決定事項ログ。1 エントリ 1〜2 行目安、原因調査の結論等は例外的に残す。

- 2026-09-06: issue #137 起票。トップページ改修（#131）の後続テーマとして分離
- 2026-09-06: route 名 `/find-path` 確定。ゲーム内容は経路プランニング制（tick 実行前に `planned-path` を組む方式）に確定
- 2026-09-06: 着手順を段階 1 ステージ → 2 bot 配置 → 3 time-control → 4 ゲーム内容深堀 に確定
- 2026-09-06: actor は box-bot に統一（`Robot01` は旧世代の暫定）。stage-05 は段階 2 まで actor 未搭載
- 2026-09-07: 遠近方式を CSS `perspective` + `rotateX`（床面台形化）へ転換。scale 補間は破棄
- 2026-09-07: 傾きは ref 経由で制御（`--floor-tilt` 直接書換え、再レンダリング回避）
- 2026-09-07: stage-05 に box-bot-01 を actor 搭載。floor に `preserve-3d`、逆 `rotateX` で直立、position 管理は stage-04 と共有
- 2026-09-07: actor は `theater/figure/box-bot` の `BoxBot01` に確定（`samples` 版は不使用）。表示領域 = 設置領域のため実測補正を全廃
- 2026-09-07: box-bot-01 の fov 自動算出を overscan 基準へ変更、`style.height` で拡縮可能に
- 2026-09-07: box-bot-01 の表示領域 = 設置領域は維持。actor サイズは `botSize` prop でマスサイズと独立させる
- 2026-09-07: 傾いた床で Canvas が設置領域より縮む問題、`<Canvas resize={{ offsetSize: true }}>` で解消
- 2026-09-07: **ゲーム操作で React 再レンダリングを起こさない方針**を確認。actor 移動は state 依存のため未達、段階 3 で ref ベースへ寄せる（design.md 懸念・リスク）
- 2026-09-07: 同一マスに複数 bot 表示可（translate でずらす）。tilt 依存の actor 位置ズレは `resize={{ offsetSize: true }}` で解決
- 2026-09-08: ページ枠は試作 `_prototypes/proto-01` として先行。route/page 化は段階 3 まで進めてから行う
- 2026-09-12: route/page 化を一旦取りやめ。page 実装は `_prototypes` 非参照方針に変更、段階 5（ゲーム性検討）を優先
- 2026-09-12: `game-evaluation` スキルで段階 1〜4 評価。「おもしろみ」該当なしの指摘を受け段階 5 追加
- 2026-09-12: 段階 5 着手。ジャンプ 3 回アンロック撤去、stage-06 の動作確認用静的 bot 削除
- 2026-09-12: 経路未選択時「実行」「1 手戻す」disabled 化、実行完了時に予定経路クリア
- 2026-09-12: 予定経路を到達ごとに 1 つずつフェードアウト。`PlannedPathCellRegistryProvider` 新設（ref + DOM 直書き、再レンダリングなし）。以降複数件、DOM 直書きが React の style diffing に乗らず再選択・一括クリア・複数回通過時に古い opacity が残るバグを順次修正（`resetCell`/`resetAllCells`/経路上残存判定）
- 2026-09-12: 走行中のセル追加が反映されない不具合を修正。`isRunning` 追加、走行中は選択・実行操作を全 disabled 化
- 2026-09-12: 同一セル複数回選択時に番号が最新化されない問題を修正。原因: セル単位の `orderByCell` が複数出現を 1 つの番号でしか表現できなかったこと。`PlannedPathCellRegistryProvider` を order（経路上の通し番号）単位へ再設計
- 2026-09-14: 表示方式 `stacked`/`list` の 2 variant を比較試作。最終的に重複選択禁止（`allowDuplicateSelection`）を採用方針とした
- 2026-09-14: 段階 5「到達済みマスのみ表示」実装。proto-02（隣接逐次移動方式）へ実装。原因: 視界制限は先読み前提の proto-01（積み上げ→実行）とは根本的に矛盾する（複数マス先読みができなくなる）。`VisibilityRegistryProvider` 新設（ref + DOM 直書き）
- 2026-09-14: CSS Grid の auto-placement が `display: none` 要素を配置計算から除外し、可視セルが詰めて再配置されるレイアウト崩れを発見・修正。`gridColumn`/`gridRow` を明示指定
- 2026-09-14: proto-02 の隣接移動判定へ斜め方向（8方向）を追加、切替可能に（`allowDiagonal`）
- 2026-09-14: 「視界」（現在地8近傍、常時可視）と「到達済み」（訪問履歴、表示 ON/OFF 切替可）を2軸に分離。視界に入ったセルは以降到達済み扱いとする
- 2026-09-14: 床タイル（`GeoLayer`）が視界外でも非表示にならない不具合を修正。`registerVisibilityNode` を `GeoLayer` にも配線
- 2026-09-14: 到達済みマスのみ表示（視界制限）の本採用可否は保留、後日判断。段階5「隣接マス制限」の採否とあわせて検討
- 2026-09-14: 隣接マス限定移動は「同じセルへ戻って通る」動線が正当な経路として発生しうるため、重複選択一律禁止は隣接制限と相性が悪いと判断。新規 `proto-02`（隣接クリックで 1 手ずつ即時移動する逐次型）を新設して比較試作。time-control の store 群・tick ループは持ち込まない
- 2026-09-14: `useAdjacentMove` を全 ref 化（`useState` 撤去）、`Stage06` 配下全体の再レンダリングを解消。`disabled` 属性の DOM 直書き切替は React の合成イベント到達不可バグを踏むため不採用、`isAdjacent` ガード方式に統一
- 2026-09-15: 「bot を進行方向へ向ける」を proto-03（hex）のみ実装。box-bot-01 に `face` action 新設（`applyYawDelta` による瞬時切替）。hex 方向 → 画面角度変換は box-bot-01 非依存の `hex.ts` に切り出し
- 2026-09-15: face の向きズレバグ修正。原因: box-bot-01 の yaw 基準と画面座標 atan2 基準が 90° ズレている上、カメラの遠近視点により両者の関係が非線形。単純オフセット補正では不十分なため、数値逆算（`screenAngleToYaw`）で補正
- 2026-09-15: 「歩くモーション再生」を proto-03 のみ実装。既存 `walkingAction` を再 export、moveActor 直後に ON・`transitionend` 検知で OFF にするトグル方式
- 2026-09-15: 歩行に腕振りを追加。`applyArmSwing` 新設、脚と逆位相（左腕=右脚）で計算
- 2026-09-15: proto-03 は 1 マス移動と歩行モーションの相性が悪いため `enableWalking` prop で切替可能に（既定 OFF）。proto-01（tick 駆動）にも歩行対応、実行全体を 1 周期として on/off
- 2026-09-15: 「bot 向き転換」を proto-01（tick 駆動）へも追加。矩形グリッドは座標変換不要、`gridDirectionToScreenAngle` 新設
- 2026-09-15: proto-03 に速度調整（`moveDurationMs`）と walking 切替 UI を追加。walking の脚振り周期 `cycleSec` を `moveDurationMs` に連動させ（低速時の視認性確保のため頭打ち `maxWalkCycleSec` を設定）
- 2026-09-15: 脚・腕振り角（`legSwingAngle`/`armSwingAngle`）をスライダー化。腕振り角は後に「常時180度固定」要望を受け撤去、`ARM_SWING_ANGLE` 定数化 → 振幅と可動域の解釈誤りが判明し `Math.PI / 2`（前後90度ずつ）に修正
- 2026-09-16: 移動距離に依らず速度一定化を案1（速度ベース化）で実装。`actor-node-registry` の `moveActor` で移動距離（斜めは√2倍）から transition duration を算出し DOM 直書き。proto-02 で直進150ms/斜め212msを実機確認
- 2026-09-16: 障害物衝突判定は案A（セル境界のみ）に確定。次マスに障害物があれば `moveActor` 自体を呼ばない方式。理由: 現 actor 移動は CSS transition 任せで JS 側がアニメ中の途中位置を持たないため、マス移動途中の急停止は不可（正確にやるなら Web Animations API 移行が要る、過剰と判断）。経路プランニング制は離散ステップのため案Aで十分
- 2026-09-16: 複数中継点の連続等速移動を案A（tick 停止ゼロ化、CSS transition 継続）で実装。`use-find-path-tick` の tick 発火間隔を固定 `TICK_MS` から区間距離ベースの可変長（`moveActor` と同じ distance × `CELL_TRANSITION_MS`）に変更、区間の継ぎ目で静止せず動き続けるようにした。旋回時の減速等の連続速度プロファイル制御（案B: Web Animations API 移行）は将来検討として design.md 懸念・リスクへ分離
- 2026-09-16: 上記実装で実機に停止が残る不具合を3件修正。(1) carry 蓄積を setInterval 呼び出し回数の固定加算でなく `performance.now()` の実経過時間ベースに変更（メインスレッドの一時ブロックでタイマー発火が遅延・間引きされると蓄積が実時間とズレるため）(2) tick 発火の閾値を「これから消化する区間自身の duration」でなく「直前に消化した区間の duration」に修正（`thresholdForSegment`。直前の移動完了を待ってから次を開始するのが正しく、自身の duration を閾値にすると継ぎ目がズレる）(3) 閾値 0（最初の区間、待機なし即時消化）を消化した際 carry を 0 にリセットするよう修正（リセットしないと tick 起算〜最初の tick 発火までの実経過時間が次区間の待機時間に食い込み早まる）。あわせて `execute` 開始直後の tick 起算を `requestAnimationFrame` 後に遅らせ、`isRunning` 変更に伴う重い再レンダリング（design.md 懸念・リスク）で最初の carry が過大に貯まり複数区間が一括消化される問題を緩和。さらに `takeWhile(hasRemaining)` を `mergeMap` の後から `scan` 直後へ移動（`consumed=0` の tick では mergeMap が何も emit せず takeWhile が評価されないため、経路消化完了後も tick ループが終了せず残り続けるバグがあった）。MutationObserver で moveActor 呼出し間隔を計測し直進150ms/斜め212ms付近に収まることを複数回確認
- 2026-09-17: `use-find-path-tick.test.ts` の timeScale 関連テスト3件不安定問題の原因判明・修正。実態は fake timer 起因でなく2点の不具合。(1) テスト側の `advanceTimersByTime` が旧実装（固定 `TICK_MS`=400ms 発火）の値のまま、2026-09-16 の区間距離ベース可変長化（`CELL_TRANSITION_MS`=150ms 基準）に追従していなかった → `CELL_TRANSITION_MS` 基準へ書き換え（3回連続実行で決定的な失敗と確認、flaky ではなかった）(2) 実装側にも潜在バグ: `scan` 内 while 条件が `carry >= threshold` で、最初の区間（閾値0、待機なし即時消化仕様）は carry=0 のままでも真になるため、`timeScale=0`（ポーズ中）でも tick 発火さえあれば最初の一歩だけ勝手に消化されていた → `scan` 内 `timeScale === 0` の場合に consumed=0 で早期 return するガードを追加。修正後 `use-find-path-tick.test.ts` 10/10 pass を3回連続確認、`find-path`/`time-control` 系テスト全21ファイル122件・`tsc --noEmit` も通過
- 2026-09-18: 回復アイテム実装（issue #181）後、「回復アイテムのせいで全マス再レンダリング」と疑われたため実測。通常マス/回復アイテム/回復スポットへの1手移動で `Stage06`/`GeoLayer`/`ActorsLayer` の render 回数を比較したところ全て同一（各2回）。原因は回復アイテムでなく既存の `isRunning` state（design.md 懸念・リスク、2026-09-07 決定事項）。「実行」開始・終了の2回で `FindPathContent` 配下全体が再レンダリングされる既存課題が、回復アイテム実装で初めて目視で気づかれただけ
- 2026-09-18: マス上オブジェクト（障害物・回復アイテム・回復スポット）へ `title` 属性 + className 付与を実装。className 共通プレフィクスは `ui-term-`（`ui-term-obstacle`/`ui-term-energy-recovery-item`/`ui-term-energy-recovery-spot`、ideas/README.md 参照）。表示レイヤー（`ObstacleLayer`/`RecoveryItemLayer`/`RecoverySpotLayer`）は `pointerEvents: none` の非対話オーバーレイで hover を受け取れないため、`title` は実際にマウスオーバーを受けるセル本体側へ持たせた（proto-01: `PlannedPathLayer` の `button`、proto-03: `Stage07`/`GeoLayer` へ新設した `cellTitle` prop 経由）。対象範囲は proto-01 の3種 + proto-03 の障害物（`OneWayLayer` は対象外）
- 2026-09-18: `isRunning`/`reachedGoal` による `FindPathContent` 配下全体の再レンダリングを解消。両 state を `useFindPathTick` の `useState` から専用 `TickStatusStore`（proto-01 固有、`items`/`energy` と同じ `createStoreContext` パターン）へ移し、`ActionBar`/`PlannedPathLayer` が props でなく selector で直接購読する形に変更（`FindPathContent` 自体は両値を扱わなくなり、値変更で再レンダリングされない）。Storybook + Playwright の console 計測で「実行」クリック〜tick 完走まで `Stage06`/`GeoLayer`/`ActorsLayer` の render ログが 0 件（従来は開始・終了で 2 回）になったことを確認
- 2026-09-19: PR #196 レビュー対応。(1) `Stage07`/`GeoLayer` の `cellTitle` prop（props drilling）を撤去し、`stage-07/_contexts/cell-title` の `CellTitleContext` へ変更。stage-07 は find-path 固有概念を持たないため型のみ定義し、実体（`getCellTitle` の中身）は呼び出し元（proto-03: `CellTitleProvider`）が注入する。GeoLayer は hook（`useGetCellTitle`。当初 `useCellTitle` としたが戻り値が関数のため命名修正、CLAUDE.md 追記済み）をループ外で1度だけ呼び出し、戻り値の関数をセルのループ内で使う（hook をループ内で呼べないため）。(2) proto-01 の `PlannedPathLayer` が独自に持っていた「isObstacleCell + アイテム検索」の分岐ロジックを廃止。当初 `ItemStore` へ障害物（`obstacleCells`）を物理的に統合したが、レビューで「ItemStore に障害物を持たせる理由がない」と指摘され撤回。障害物（`_lib/obstacle.ts`、静的定数）とアイテム（`ItemStore`）は責務を分けたまま独立管理し、`_lib/get-cell-contents.ts` の `getCellContents(cell, itemState)`（pure function）が両者を横断的に問い合わせる形に修正。hover 説明文言は `describeCellContent` で解決、アイテムの文言・絵文字・className は `_lib/item-presentation.ts` の `getItemPresentation`（`ItemKind` ベースの辞書）に一元化。(3) `RecoveryItemLayer`/`RecoverySpotLayer`（種類ごとにレイヤーが増える設計だった）を `ItemLayer` 1つへ統合、表示は `getItemPresentation` で解決するため種類が増えてもレイヤー自体は増やさない
- 2026-09-22: 「障害物 / 歩数制限 / 一方通行セル」は3要素とも実装済み（障害物 PR #176、一方通行セル PR #180、エネルギー issue #181）。backlog.md から close
- 2026-09-22: 段階 5「1 手戻す」と「戻る」の別枠操作分離はインセンティブ設計が未確立のまま対応なしとし、backlog.md から削除
- 2026-09-22: proto-03 `EnergyDebugPanel` 操作時の `Stage07` 配下ツリー全体再レンダリングを解消（PR #212）。非対話マーカー系レイヤーの `React.memo` 化、`MoveTargetLayer` の EN store 直接購読化、`ActorsLayer`/`GeoLayer` の `React.memo` 化 + `handleArrived` 安定化で対応。backlog.md から close
- 2026-09-22: mob の EN 個別化リファクタ着手前に調査したところ、`ActorsLayer` 自体が単一 actor 限定設計（複数 actor 対応は issue #162 で対象外と明記済み）で、mob 実装は現状ゼロと判明。mob 表示の土台がまだ無いため EN 側だけ actorId 対応させるのは投機的抽象化になりうる点をユーザーへ確認、EN 個別化のみ先行する方針で継続
- 2026-09-22: 上記を受け EN 個別化を実装。`outOfEnergyRef`（mob ごとの `useEnergyEventListener` 購読 + `useRef`）を廃止し、`useOutOfEnergyEventListener`（`_events/_event-listeners`、scope 全体で 1 回だけマウント）へ一元化。発火中フラグは `Record<ActorId, boolean>` の ref で持ち、`event.detail.actorId` をキーとして直接読み書きする。mob 側の `energyOut` dispatcher は新設レジストリ（`_events/_registries`、`WeakMap<EventTarget, Map<ActorId, () => Promise<void>>>`）へ `useRegisterEnergyOut(actorId, energyOut)` で登録するだけでよい設計にした。旧実装は ts-pattern の `match({ event: { detail: { actorId } } }, ...)` で「自分宛てか」を判定していたが、これはオブジェクトパターン内の変数キャプチャであり実質どの actorId でもマッチしていた疑いがある（単一 actor 運用のため実害なし）。新実装は素直なキー参照のため同種の不具合が起きない。`EnergyDebugPanel`/`canEnterCell` の `PLAYER_ACTOR_ID` 決め打ち、`ActorsLayer` の複数 actor 対応は今回のスコープ外のまま backlog.md に残す
- 2026-09-22: 上記実装を SOLID 原則・複数 actor 対応の観点でユーザーとレビュー。S/O/D 各原則に沿っている、`WeakMap<EventTarget, ...>` によるレジストリのスコープ分離は `EnergyStoreProvider` のリセット時再マウント（`key` 付き、EventTarget も再生成される）に対して世代分離が効くため過剰な一般化ではないと判断。一方 `useBoxBotActionDispatcher` の戻り値が `useMemo` 化されておらず毎レンダー新規オブジェクトになる（`energyOut` 参照不安定 → `useRegisterEnergyOut` の effect が無駄に再実行）点を発見、mob 未実装の現状は実害なしとして修正見送り、backlog.md へ残項目として記録
- 2026-09-22: 上記 `useBoxBotActionDispatcher`（box-bot-01 版）を `useMemo` 化して対応。呼び出し側（`stage-07/index.tsx`）が `actions` 引数を毎レンダー新規配列リテラルで渡すため、`actions` 参照でなく action 名を `|` 結合した文字列（`actionsKey`）を dep にして内容比較化。backlog.md から close
- 2026-09-22: backlog.md「mob配置」項目を GitHub 上のサブ issue(#215)として分離し、`issue-137-top-page-transition-target/_issues/issue-215-mob-multi-actor/` へ切り出し
- 2026-09-23: proto-03 の非隣接クリック対応を段階分割して着手。旧 stage-04 design.md 記載の「BFS 実装」は実体（コード）が存在せず、移植ではなく新規実装と判明。まず「隣接/非隣接の判定」のみの最小実装から開始し、通知表示（`useNotifications`、右スライドイン + autoDismiss、PR #219）・進入可能セルのカーソル変更（隣接制約を外し `canEnterCell` のみで判定）まで実装。経路探索（BFS）による自動移動は次段階として backlog.md に残す
- 2026-09-23: 上記の続き。経路探索(BFS)本体とプレビュー表示を先行実装、自動移動の実行はさらに次段階へ分割（ユーザー方針: 経路プレビュー表示から着手）。`stage-07/_lib/hex-path.ts` の `findHexPath` を新設（`isHexCellInGrid` を export 化して利用）。`canEnter` は `(from, to) => boolean` の形にした。一方通行判定(`isBlockedByOneWay`)が移動元セルへ依存するため、探索中に動く `from` を固定できない（コンポーネント内の `enterGuards`/`canEnterCellPerceived` は `currentCell` に固定されたクロージャで使い回せない）。EN 残量は経路の各手で消費されていく動的資源のため経路の「形」自体には含めず、静的な障害物・一方通行のみで判定する（不足時の扱いは自動移動実装時に検討）。表示は `PathPreviewLayer`（`ObstacleLayer` 同型の非対話レイヤー）を新設、`registerVisibilityNode` は持たせず到達済み表示 ON/OFF に関わらず常時表示する（経路は未到達セルを通ることがあるため、他レイヤーの「未到達マスは隠す」方針の意図的な例外）
- 2026-09-23: 中継点機能の設計方針をユーザーと確認。(1) 経由順は「クリック順」でなく設置箇所が近い同士を自動接続(最近傍法)、厳密な最適解(TSP)までは求めない(必要なら中継点を増やして対応する運用でよい)。(2) 通常モードと中継点選択モードのセルクリックは別イベントとして完全分離する。`event-driven-actions.md` の手順に従い、まず state 切替の配線のみを実装（`waypointFlowState: 'idle' | 'proposing' | 'selecting'`、`waypoints: HexCell[]`）。選択モード中は `Stage07` の `interactive={false}`（既存の「クリックを外側レイヤーへ委ねる」仕組み）を利用し、新設 `WaypointSelectLayer` がセルクリックを拾う。中継点は同一セル再クリックで配列から除去(toggle)
- 2026-09-23: `WaypointBubble`（思考吹き出し）を当初 bot 頭上（`Stage07` の 3D 空間内、`translateZ` で floor 平面より手前へ押し出し）に実装したが、Playwright での実機検証で意図しないセル（隣接セル、EN 消費を伴う実移動）がクリックされることが判明。`document.elementFromPoint` で調べると実際のヒット要素は `GeoLayer` の SVG `polygon`（floor 平面上、z=0）で、`translateZ(10px)` を加えても効かなかった。`rotateX` + `preserve-3d` 環境のブラウザ奥行きヒットテストが DOM 順・z 値のどちらにも従わないケースがあると判断（`GeoLayer` の「3D ヒットテストが DOM 順を無視することがある」という既存コメントと符合する新たな事例）。対応として `WaypointBubble` を `Stage07` の外側（操作パネル、通常の 2D DOM）へ表示する形に変更、3D 空間内での bot 頭上表示は見た目を作り込む次段階で再検討する
- 2026-09-23: 中継点フローの表示制御（`WaypointBubble`/`WaypointSelectLayer`/選択中インジケータの表示/非表示）を `src/hooks/use-css-toggle`（`useCssToggle`、proto-01 の「歩く」ボタン解放で採用済みの hidden checkbox + vanilla-extract `:checked` セレクター方式）へ置き換え。ユーザー方針: 表示/非表示要素のみ CSS 化する（`Stage07` 改修による完全な ref/CSS 駆動化までは踏み込まない）。`waypointFlowState`(React state)自体は `Stage07` の `interactive` prop(React props 経由、CSS だけでは制御不可)を駆動するため残し、`setWaypointFlow` ヘルパーで state 更新と 3 つの `useCssToggle` インスタンスの `set` を同時に呼ぶ形にした。`WaypointSelectLayer`（`Stage07` の 3D 空間内）と選択中インジケータ（操作パネル、通常 DOM）は異なる親配下のため vanilla-extract の兄弟セレクター(`~`)を共有できず、`selecting` 用に別々の checkbox が要る（3 インスタンス構成）。各表示コンポーネントは常時マウントしたまま `className` で表示切替するよう改修（`WaypointSelectLayer` は 25 個のボタンを `<div className={...}>` でラップ）。実機検証で idle 時点から checkbox 3 個・`WaypointSelectLayer` ボタン 25 個が常時 DOM に存在すること、表示切替が正しく動くことを確認
- 2026-09-23: ユーザーから中継点機能への指摘3件。(1) bot 頭上非表示は意図通り(前回の 3D ヒットテスト回避の結果、既知)。(2) 「中継点が選択できないように見える」→ Playwright 実機検証の結果クリック自体は正しいセルに届いており、設置操作自体は成功していたが、見た目のフィードバックが皆無(テキストの件数表示のみ)だったための体感バグと判明。対応: `WaypointSelectLayer` に `waypoints` prop を追加、設置済みセルへ 📍 マーカーを表示するよう前倒し実装(当初 backlog では次段階予定だった)。(3) `selecting` モードから抜ける操作が存在しなかった(確実なバグ)。対応: 「完了」ボタン(`handleWaypointDoneClick`)を追加、`waypointFlowState` を `'idle'` へ戻す(`waypoints` はクリアしない)
- 2026-09-23: 上記(2)のマーカー実装中、副次的な設計漏れを発見。`WaypointSelectLayer` は全 25 セルにボタンを持つため、`VisibilityRegistryProvider` の可視制約(視界内 or 到達済み表示ON時の到達済みセルのみ)を受けておらず、視界外(未探索)セルにも中継点を設置できてしまっていた（実機で「グリッドから離れた位置にマーカーが表示される」ように見えたが、原因は 3D perspective 投影上正しい座標であり、実際には視界外セルだった）。対応: `visibility-registry` の `NodeKind` に `'waypoint'` を追加（`'marker'` は `GoalMarkerLayer` 等の疎な配置前提のため、全セルに存在する `WaypointSelectLayer` と同一セルで衝突し `Map<NodeKind, HTMLElement>` の後勝ち上書きが起きる、既存 `marker` は流用不可と判断）。`WaypointSelectLayer` に `registerVisibilityNode` prop を追加し各ボタンを `kind: 'waypoint'` で登録。あわせて、visibility registry は `el.style.display` を直接書き換える方式のため、React 側の `style` に `display` プロパティを含めると競合しうる（`GeoLayer` は元々 `display` を持たせていない）。マーカーの絵文字中央寄せに使っていた `display: 'flex'` を `textAlign`/`lineHeight` へ置き換えて回避
- 2026-09-23: ユーザーから「bot 頭上に思考吹き出しを表示したい」と再依頼（前回は 3D ヒットテスト問題で操作パネル(2D)へ回避済み）。`translateZ` の値を実機で段階的に検証したところ、10px/50px では奪われ続けるが 100px から自身がヒットすると判明（安全マージンを見て 120px を採用、`WaypointBubble` の `HIT_TEST_SAFE_TRANSLATE_Z_PX` 定数化）。bot 頭上 3D 配置へ戻す過程で追加のバグ2件を発見・修正。(a) `useCssToggle` の生成クラス名は全インスタンス共通のため、同じ親(floor)内に `waypointSelectLayerCheckbox`/`waypointBubbleCheckbox` を並べると vanilla-extract の `~` セレクタが「前方のいずれかの checked な checkbox」に反応してしまい、selecting 中も吹き出しの opacity が 0 に戻らないバグがあった(Storybook `SharedScope` ストーリーが警告する既知の制約が実際に踏まれた例)。対応: ペアごとに div で親を分離。(b) その分離用 div 自体に `position: 'absolute'` が無いと通常のブロック要素としてドキュメントフローに乗ってしまい内部の絶対位置計算の基準がズレる、`transformStyle: 'preserve-3d'` が無いと子孫の `translateZ` が平坦化され 3D ヒットテスト回避ごと効かなくなる、`pointerEvents: 'none'` が無いと idle 時もこの div 自体が floor 全体を覆う透明な層として `GeoLayer` のクリックをブロックしてしまう、の3点が必要と判明（実機検証の反復で発見）。残課題: `translateZ(120px)` の押し出しにより perspective 投影で吹き出しの見た目位置が bot 真上からやや上方にズレる副作用が残っている（許容範囲かはユーザー確認待ち）
- 2026-09-23: 上記のズレをユーザーへ報告したところ、位置確認を優先する方針に転換。「div でコンポーネント作成を想定」「bot の頭 10px ほど上に border を指定した div を表示してみて」という指示を受け、`WaypointBubble` を button→div（`role="button"`/`tabIndex={0}`/`onClick` 維持）へ変更、`translateZ` の押し出しは一旦外し、位置計算を「セル中心 - botSize - 10px」（`botSize` は `Stage07` と共有する `BOT_SIZE` 定数）に変更。実機確認で bot 頭のすぐ上に border 付き div が表示されることを確認、ユーザーから「いったん ok」
- 2026-09-23: 「コンポーネントが肥大化していっているのでまずは吹き出しについて `_components` に切り出し」との指示。`WaypointBubble` が自身で `useCssToggle`（checkbox・wrapper div・`useEffect` による `visible` prop → checked 同期）を持つ自己完結コンポーネントへリファクタ。親(`index.tsx`)は `visible={waypointFlowState === 'proposing'}` を渡すだけになり、`waypointBubbleCheckbox`/`setWaypointBubbleVisible`/`waypointBubbleClassName` 関連の分割代入・`setWaypointFlow` 内の呼び出し・専用 wrapper div を削除できた。実機検証で表示/非表示の CSS 駆動は正常動作を確認。クリック判定（3D ヒットテストに `GeoLayer` セルへ奪われる問題）は `translateZ` を外した現状のまま未解決で残っている（前回合意通り「見た目確定後に再検討」のスコープ外、今回のリファクタでは劣化させていないことを確認）
- 2026-09-23: 「今回の対応については切り出すこととする」との指示を受け、`WaypointSelectLayer`・選択中インジケータ+「完了」ボタン（新設 `WaypointSelectingIndicator`）も `WaypointBubble` と同型のパターンで自己完結コンポーネント化。`index.tsx` から `useCssToggle` の import・3インスタンス分の分割代入・`setWaypointFlow` ヘルパー(もはや `setWaypointFlowState` と同義になったため削除、各ハンドラは直接 `setWaypointFlowState` を呼ぶ)・2つの専用 wrapper div を全て削除、`waypointFlowState` から `visible` prop を計算して各コンポーネントへ渡すだけの形になった。実機検証（idle 時の checkbox 3個・waypoint ボタン25個の常時マウント、proposing/selecting 各表示、中継点設置、完了ボタンでの復帰）で全て正常動作を確認。3D ヒットテストによるクリック判定の制約は変わらず残っている（既知の未解決課題、今回のスコープ外）
- 2026-09-23: 「マスの上に吹き出しを出すと何かが上にかぶっている、z-index で調整したい」との指摘を実機検証。(1) 3D `preserve-3d` 環境内では `z-index` 自体が無効（追加しても重なり変わらず）。(2) `translateZ` 押し出しも試したが、tilt 打ち消しの `rotateX` と組み合わさると必要な押し出し量が z=300px 相当まで膨らみ、見た目の位置が大きくズレる（x:63→17, y:404→431）。原因は tilt 打ち消し回転で吹き出しが floor 平面から「起き上がった」状態になり、`GeoLayer` の傾いた平面と 3D 空間内で交差するため（交差する2平面は視点により前後関係が場所ごとに変わり、単純な奥行き指定では制御できない）。z-index の追加は撤去し、確実な解決には吹き出しを floor の 3D 空間の外に出す必要があると判断
- 2026-09-23: 位置調整を optimize しやすくするため `WaypointBubble` の Storybook story に tilt スライダーを追加。`Stage07` の `floor`(`perspective` + `rotateX(var(--floor-tilt))`)と同じ構造を再現、bot 自身も `ActorsLayer` と同じ逆 `rotateX` で打ち消す。tilt 0/55/85度いずれでも吹き出しが水平を保つことを実機確認
- 2026-09-23: 「修正の方針を決定した」との指示で story を decorator(`meta.decorators`)形式から、通常の関数コンポーネント(`BubbleStage`)を各 story の `render` から呼ぶ形式へ書き換え。挙動・見た目は変更なし（次段階の修正をしやすくするための整理）
- 2026-09-23: 上記を踏まえ、重なり問題の根本対応を `createPortal` 方式に決定（ユーザー提案）。`bubble は複数を想定` との指摘も反映し、コンテナは 1 つの DOM のみを公開し呼び出し側が必要な数だけ `createPortal` を呼べる設計にした。`ActorsLayer`/`Stage07` に `registerPlayerOverlayContainer`（コールバック ref、`registerVisibilityNode` と同じパターン）を追加、player bot の位置決め div 内にオーバーレイ注入用コンテナ DOM を用意して公開する。`stage-07` は find-path 固有の概念を持たない設計方針を維持（`CellTitleProvider` と同種のパターン、コンテナを渡すだけで中身の注入は呼び出し側に委ねる）。`WaypointBubble` 自身は座標計算(`botSize`/`cols`/`currentCell`/`hexSize`/`rows`)を全廃し、`className` のみで位置指定する設計に変更（推奨位置は `WAYPOINT_BUBBLE_POSITION_CLASS_NAME` としてコンポーネント側からexport、`index.tsx`/story 両方から参照して重複を避ける）。実機検証で重なり問題の解消（吹き出しの枠が最前面に表示）を確認、selecting 移行・中継点設置/除去・完了ボタンでの復帰も含め一連のフローが正常動作することを確認
- 2026-09-23: 「複数 actor がいる場合 `registerPlayerOverlayContainer` を複数指定することになるか」との質問を受け現状確認。当時の実装は player 専用の単一 props で mob には未対応と回答。続けて「actor に紐づく実装なので props drilling でなく actors store に格納し actorId で取得する方が良いのでは」との提案を受け同意、実装した。`ActorsState` に `overlayContainers: Partial<Record<ActorId, HTMLDivElement>>` と `registerOverlayContainer(actorId, el)` アクションを追加（`el=null` での解除は既存 `despawnActor` と同じ `filter` パターン、destructure での未使用変数を避けるため）。`ActorsLayer`/`Stage07` の `registerPlayerOverlayContainer` prop（Stage07→ActorsLayer の1ホップの props drilling）を削除し、`ActorsLayer` が `useActorsStore` 経由で直接 store へ登録、find-path 側（`index.tsx`）も同じ `useActorsStore` から `overlayContainers[PLAYER_ACTOR_ID]` を直接 selector 購読する形に変更。actorId をキーにしたことで、将来 mob（issue #215）にオーバーレイを付けたくなった場合も同じ仕組みで自然に対応できる。store のテストに `registerOverlayContainer` の登録/解除ケースを追加、実機検証・全ユニットテスト通過を確認
- 2026-09-23: `WaypointBubble` の表示調整（未選択時=丸コネクタ2-3個をふわふわアニメーション、選択時=bot向き三角形のしっぽ+文言「中継？」→「中継点！」）を実装。`selectable` は props でなく `forwardRef`+`useImperativeHandle`（`WaypointBubbleHandle.setSelectable`）経由の imperative 命令に変更（ユーザー方針: props 結合を減らし、値変化のたび新しい props を流し込まない）。CSS は vanilla-extract の hidden checkbox + 兄弟セレクター方式（`useCssToggle` と同型）だが、`useCssToggle` が返す checkbox は全インスタンス共通クラスのため他 `.css.ts` から参照できず（既出の `SharedScope` 制約）、`selectable` 用はコンポーネント専用の checkbox クラスを自前実装（`useCssToggle` は使わず ref 直書き）。実装中に2件の見た目バグを発見・修正: (1) `.${bubbleButton}` のようにセレクター内で vanilla-extract の style 参照へ手動で `.` を付けると二重ドットになり無効セレクターになる（正しくは `${bubbleButton}` のまま埋め込む）、(2) bubble 本体の button に残っていた `position: absolute` が丸/しっぽの flex レイアウトと衝突し、実際の描画位置がズレていた（削除して解消）。`visible` の表示条件（`waypointFlowState === 'proposing'` のみ、selecting 中は非表示のまま）は今回のスコープ外としてユーザー承認の上で現状維持
- 2026-09-23: 上記の初版に対しユーザーからフィードバック（(1) ふわふわは丸でなく本体、(2) 丸はbotと本体をカーブでつなぐ位置に、(3) 三角形の向きを固定下向きでなくbot方向にしたい、(4) それには本体の位置指定自体を数値化する必要がある）を受け再実装。`className`(tailwind文字列)による位置指定を `offset: { x: number; y: number }`(bot基準点(0,0)からのpx)へ変更（`WAYPOINT_BUBBLE_POSITION_CLASS_NAME` → `WAYPOINT_BUBBLE_OFFSET`）。丸の弧・三角形の座標は `offset` の逆ベクトルをbot方向とみなし `_lib/compute-connector-geometry.ts`（pure function、二次ベジェ曲線）で算出、SVG(`circle`/`polygon`)で描画する形に全面書き換え（丸はCSS flexでなくSVG上のベジェ曲線サンプリング点、三角形は固定border-trickでなくbot方向を向くpolygon）。ふわふわ(上下+微小回転)は`connectorDot`から`bubbleButton`(本体)へ移設、回転中心は`transformOrigin: bottom center`(コネクタ側)でズレを抑制。SVGの`width/height/viewBox`を省略(0起点)すると`overflow:visible`のCSSだけでは描画されないバグを実機検証で発見、`viewBox`を原点中心の正方形(`CONNECTOR_SVG_SIZE_PX`定数、80px)で明示して解消。Storybook `Interactive` story に offset スライダーを追加し、位置を動かしてもコネクタが自動的にbot方向へ追従することを実機確認
- 2026-09-23: 追加フィードバック(丸の弧の膨らみ方向を逆に、丸を本体に近いほど大きく、丸を大きく+ふわふわ、丸の間隔を広げる、hover時dotted→solid、selectable時はふわふわ停止+border常時solid+三角形を本体近くへ)を順に反映。selectable時の三角形は丸のREACH_PX(bot方向への到達距離)を共有せず、専用の短い`TRIANGLE_REACH_PX`(16px)を新設して本体寄りに配置
- 2026-09-23: proto-03 実機で「default(未選択)状態が hover していないとき見づらい」と指摘。「クリックはリップル、グラデーションは非hover時常駐で存在アピール」の方針で追加実装。(1) hover中は `animation-play-state: paused` でふわふわ揺れを止める(headless実機検証ではhover自体は機能していた、揺れる本体にマウスを合わせ続けられない体感問題と判断して対策)。(2) 丸を塗りつぶしから白背景+グレー枠線(stroke)へ変更。(3) 本体(button)の背景に、細く薄いグレーの帯が右から左へ一方向に流れ続けるアニメーションを追加(往復でなく一方向ループ、`linear`で等速)。カラーストップは当初広め/濃いめで実装しユーザーフィードバックを受け段階的に絞り込んだ(最終: 46%-54%の8%幅、`rgba(156,163,175,0.5)`)。(4) クリック時の波紋(ripple)を追加。CSS クラス側に `animation` を持たせると、reflow トリック(`style.animation = 'none'` → reflow → `''` に戻す)で振り出しに戻す際 CSS 側の値へ戻ってしまい2回目以降のクリックで再生されなくなるバグを発見、`ripple` クラス自体は `animation` を持たず `opacity: 0` のみとし、`index.tsx` がクリックのたびインライン `style.animation` へ keyframes 名を直接設定する方式に修正して解消
- 2026-09-23: 実機確認中「selecting モードへ移行すると WaypointBubble が非表示になる(selectable の見た目が実際には見えない)」との指摘。以前 backlog.md へ「次段階」として送っていた `visible` 条件の制約（`waypointFlowState === 'proposing'` のみ）をこのタイミングで解消する方針を確認、`visible={waypointFlowState !== 'idle'}` へ変更。selecting 中も bubble が表示され続け、発言吹き出し(三角しっぽ+文言「中継点！」)が実際に見える状態になったことを実機確認
- 2026-09-23: 「吹き出しがマスと重なっているとクリックできない」との指摘を受け調査。Playwright で `WaypointBubble` のルート div の `style.left`/`style.top` を直書きし、意図的に GeoLayer の polygon(セル)へ重なる位置へ移動させて実機再現。重なった状態で `document.elementFromPoint` が `button`(bubble)でなく `polygon` をヒットし、実際にクリックしても bubble の onClick は発火しなかった(重ならない位置では正常に bubble がヒットする)。原因は既出（2026-09-23 の複数エントリ）の「`rotateX`+`preserve-3d` 環境のブラウザ奥行きヒットテストが見た目の重なり順(z-index/DOM順)に従わないケースがある」問題と同根。bot 頭上の overlayContainer(`ActorsLayer` の位置決め div 内)が Stage07 floor の preserve-3d 空間の子孫であるため、GeoLayer のセルと同じ3D空間内でヒットテストが競合する。対策候補を3つ検討: (1) overlayContainer を floor の 3D 空間外へ出す(根本対応、bot 追従を JS 座標計算に置き換える必要があり影響大) (2) pointerdown を document capture フェーズで座標ベース判定し強制的にクリックを成立させる(局所対応、実装小さいが「見えている場所と違う判定で反応する」新種のバグ源になりうる) (3) `translateZ` 押し出しは過去に tilt との組合せで位置ズレが起き断念済み、再挑戦は非推奨。ユーザー判断で (1) を採用方針に決定。本 PR(#224)はここまでとし、実装は次の PR で対応する
- 2026-09-23: 上記 (1) を実装。overlayContainer を `ActorsLayer`(floor 内)から新設の `ActorOverlayLayer`(`Stage07` の scene の後ろ、floor の `preserve-3d` 空間外)へ移し、`ActorsLayer` の位置決め div 内には追従先のアンカー DOM のみ残す(store に `overlayAnchors`/`registerOverlayAnchor` を追加、呼び出し側 API の `overlayContainers[actorId]` は不変)。追従は「座標計算の再実装」でなく rAF でアンカーの `getBoundingClientRect` を実測し、コンテナの `transform` へ直書きする方式を採用。hex 座標→画面座標の計算を perspective・tilt 込みで二重実装せずに済み、セル移動の CSS transition・tilt 変更の両方へ自動で追従するため。コスト対策として注入なし(子要素 0)のコンテナは計測スキップ、値不変時は書込みスキップ。floor 外に出たため吹き出しは遠近による拡縮を受けなくなった(UI 要素のため許容)。Playwright で bubble を GeoLayer セルへ重ねた状態でクリックし、修正前は selectable へ移行せず、修正後は移行することを確認
